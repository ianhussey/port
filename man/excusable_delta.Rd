% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excusable.R
\name{excusable_delta}
\alias{excusable_delta}
\title{Smallest reporting imprecision that could excuse a matrix}
\usage{
excusable_delta(R, tol = 1e-06)
}
\arguments{
\item{R}{A symmetric numeric correlation matrix with unit diagonal.}

\item{tol}{Bisection tolerance for the threshold.}
}
\value{
A single number: the smallest excusing half-width \code{w}.
}
\description{
The excusable delta of a reported correlation matrix is the smallest uniform
rounding half-width \code{w} at which the box \verb{[R_ij - w, R_ij + w]} (unit
diagonal, clipped to \verb{[-1, 1]}) admits a positive semidefinite matrix. It
unifies the verdict and the severity into a single interpretable statistic in
the units of reporting precision:
}
\details{
\itemize{
\item the matrix is impossible at reporting precision \code{delta} \strong{iff}
\code{excusable_delta(R) > delta};
\item a value like \code{0.04} reads as "the reported values are inconsistent with any
correlation matrix unless each entry were mis-stated by more than ±0.04 --
not excusable even by rounding to 1 decimal place (±0.05 would be needed)".
}

For a matrix that is already possible at fine precision the threshold is ~0.
The value is located by bisection over the box feasibility oracle (witness
refutation + Rump-verified POCS), to tolerance \code{tol}.
}
\examples{
R <- matrix(c(1, 0.9, 0.9, 0.9, 1, -0.9, 0.9, -0.9, 1), 3, 3)
excusable_delta(R)   # far above 0.005: 2dp rounding cannot excuse it
excusable_delta(diag(3))   # ~0
}
\seealso{
\code{\link[=check_corr_psd]{check_corr_psd()}}, \code{\link[=localize_psd_fault]{localize_psd_fault()}} (whose severity carries
the same quantity as \code{excusable_delta})
}
