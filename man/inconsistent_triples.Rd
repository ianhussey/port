% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/localize-triples.R
\name{inconsistent_triples}
\alias{inconsistent_triples}
\title{Scan for rounding-robust inconsistent triples}
\usage{
inconsistent_triples(R, decimals = 2, delta = NULL)
}
\arguments{
\item{R}{A symmetric numeric correlation matrix with unit diagonal.}

\item{decimals, delta}{Rounding precision, as in \code{\link[=check_corr_psd]{check_corr_psd()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per inconsistent triple: columns
\code{i}, \code{j}, \code{k}, and \code{box_max_det} (the sound upper bound on the box-max
determinant; negative means inconsistent). Empty if none.
}
\description{
For every triple of variables, test whether the induced 3x3 correlation
submatrix can be positive semidefinite for \emph{some} assignment within the
rounding boxes. Uses a sound interval bound on the 3x3 determinant, so a
flagged triple is inconsistent even after accounting for rounding. Pure
arithmetic: no solver required. This is component B of \code{\link[=localize_psd_fault]{localize_psd_fault()}}
exposed on its own.
}
\examples{
R <- matrix(c(1, 0.9, 0.9, 0.9, 1, -0.9, 0.9, -0.9, 1), 3, 3)
inconsistent_triples(R, decimals = 2)
}
\seealso{
\code{\link[=localize_psd_fault]{localize_psd_fault()}}
}
