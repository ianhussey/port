% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{check_corr_psd_batch}
\alias{check_corr_psd_batch}
\title{Screen a corpus of correlation matrices}
\usage{
check_corr_psd_batch(
  mats,
  decimals = 2,
  delta = NULL,
  tau = NULL,
  on_error = c("row", "stop"),
  quiet = FALSE
)
}
\arguments{
\item{mats}{A list of matrices (each a candidate correlation matrix). May be
named; names become the \code{id} column (otherwise a positional index is used).}

\item{decimals, delta, tau}{Passed through to \code{\link[=check_corr_psd]{check_corr_psd()}}.}

\item{on_error}{How to handle a matrix that fails validation: \code{"row"}
(default) records a verdict of \code{"error"} with the message; \code{"stop"}
re-raises the error.}

\item{quiet}{If \code{TRUE}, suppress the escalation-frequency message.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per input matrix and columns
\code{id}, \code{verdict}, \code{tier}, \code{margin}, \code{b_upper}, \code{delta}, \code{p}, and \code{message}.
}
\description{
Run \code{\link[=check_corr_psd]{check_corr_psd()}} over a list of matrices and collect the verdicts in a
tidy table, for screening a corpus of reported correlation matrices. The
\code{pocs} escalation tier is expected to fire rarely; its frequency is logged
(as a message) so you can monitor how often the cheap rigorous tiers were
insufficient.
}
\examples{
good <- diag(3)
bad  <- matrix(c(1, 0.9, 0.9, 0.9, 1, -0.9, 0.9, -0.9, 1), 3, 3)
check_corr_psd_batch(list(good = good, bad = bad), decimals = 2)

}
\seealso{
\code{\link[=check_corr_psd]{check_corr_psd()}}
}
