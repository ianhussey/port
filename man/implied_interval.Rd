% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/implied-interval.R
\name{implied_interval}
\alias{implied_interval}
\title{Implied interval for a correlation cell (and single-cell imputation)}
\usage{
implied_interval(
  R,
  cells = NULL,
  decimals = 2,
  delta = NULL,
  hold = c("points", "box"),
  verify = TRUE
)
}
\arguments{
\item{R}{A symmetric numeric correlation matrix with unit diagonal. Up to one
off-diagonal entry per interrogated cell may be \code{NA} (missing); all other
off-diagonals must be finite.}

\item{cells}{Optional 2-column matrix (or length-2 vector) of \verb{(i, j)} cells
to interrogate. Defaults to the \code{NA} off-diagonal cell(s) if any.}

\item{decimals, delta}{Rounding precision (as in \code{\link[=check_corr_psd]{check_corr_psd()}}); used for
the \code{required_edit} of reported cells and for the box path.}

\item{hold}{\code{"points"} (default) holds the other cells at their reported
values -- a fast closed form and the cleanest partial-correlation reading;
\code{"box"} lets the other cells roam their rounding boxes (rigorous, reuses the
box feasibility oracle).}

\item{verify}{Passed to the box oracle when \code{hold = "box"}.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{i}, \code{j}, \code{reported}, \code{lo},
\code{hi}, \code{required_edit} (signed distance from the rounding box to \verb{[lo, hi]};
\code{NA} for missing cells), \code{hold}, and \code{status} (\code{"feasible"}, \code{"empty"}, or
\code{"na"}).
}
\description{
Compute the interval a correlation cell could occupy for the matrix to be a
valid (PSD) correlation matrix, given the other cells. Two uses:
interrogating a \emph{reported} cell (how far outside its rounding box it would
have to move), and imputing a genuinely \emph{missing} (\code{NA}) cell (the interval it
must have occupied -- van Tilburg & van Tilburg 2023, Use 3).
}
\examples{
R <- matrix(c(1, 0.5, 0.5, 0.82,
              0.5, 1, 0.5, 0.82,
              0.5, 0.5, 1, 0.82,
              0.82, 0.82, 0.82, 1), 4, 4)
R[1, 4] <- R[4, 1] <- NA           # pretend this correlation was unreported
implied_interval(R)
}
\seealso{
\code{\link[=localize_psd_fault]{localize_psd_fault()}}
}
