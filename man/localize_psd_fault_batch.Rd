% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/localize-batch.R
\name{localize_psd_fault_batch}
\alias{localize_psd_fault_batch}
\title{Batch fault localization over a list of matrices}
\usage{
localize_psd_fault_batch(
  mats,
  decimals = 2,
  delta = NULL,
  verify = TRUE,
  sparse_k = 3L,
  quiet = FALSE
)
}
\arguments{
\item{mats}{A (optionally named) list of correlation matrices.}

\item{decimals, delta, verify, sparse_k}{Passed to \code{\link[=localize_psd_fault]{localize_psd_fault()}}.}

\item{quiet}{If \code{TRUE}, suppress the verdict-class summary message.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{id}, \code{localization_verdict},
\code{implicated}, \code{severity_max}, \code{severity_frob}, and \code{message}.
}
\description{
Run \code{\link[=localize_psd_fault]{localize_psd_fault()}} over a corpus and collect the localization
verdicts in a tidy table. A per-class summary of how often each verdict
occurs is logged as a message.
}
\examples{
triad <- matrix(c(1, 0.9, 0.9, 0.9, 1, -0.9, 0.9, -0.9, 1), 3, 3)
localize_psd_fault_batch(list(triad = triad, ok = diag(3)))
}
\seealso{
\code{\link[=localize_psd_fault]{localize_psd_fault()}}
}
